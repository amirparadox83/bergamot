# Privacy Policy — Bergamot

**نسخه:** پیش‌نویس (Draft)  
**تاریخ آخرین به‌روزرسانی:** 2026-08-24  
**زبان:** فارسی (متن انگلیسی در ادامه)

---

## مقدمه

برگاموت (Bergamot) یک اپلیکیشن موبایل سلامت و سبک زندگی است که با اصول زیر ساخته شده است:

- **آفلاین-محور (Offline-first):** تمام داده‌های شما روی دستگاه شما ذخیره می‌شوند، نه روی سرورهای ما.
- **بدون هوش مصنوعی:** هیچ‌گونه AI، LLM یا Machine Learning استفاده نمی‌شود.
- **بدون ارسال داده سلامتی:** اطلاعات سلامتی شما (وزن، خواب، تغذیه، تمرین و...) هرگز از دستگاه شما خارج نمی‌شوند.

این سند توضیح می‌دهد که اپلیکیشن چه داده‌هایی ذخیره می‌کند، کجا ذخیره می‌شود، چه دسترسی‌هایی به سیستم‌عامل دارد و چگونه می‌توانید داده‌های خود را کنترل کنید.

---

## ۱. داده‌هایی که ذخیره می‌شوند

### ۱.۱ داده‌های سلامتی شما

همه داده‌های زیر به‌صورت محلی در دیتابیس SQLite دستگاه شما (`bergamot.db`) ذخیره می‌شوند. این داده‌ها هرگز به‌صورت خودکار به سرور ارسال نمی‌شوند:

| نوع داده | مثال | مدت نگهداری پیش‌فرض |
|---|---|---|
| پروفایل کاربر | سن، قد، وزن اولیه، جنسیت، هدف، سطح فعالیت | تا زمان حذف توسط کاربر |
| وزن بدن | ثبت روزانه وزن و درصد چربی | تا زمان حذف توسط کاربر |
| خواب | مدت و کیفیت خواب هر شب | تا زمان حذف توسط کاربر |
| تغذیه | غذاهای مصرفی، تعداد سروینگ، کالری و ماکروهای هر وعده | تا زمان حذف توسط کاربر |
| آب | میزان آب مصرفی روزانه | تا زمان حذف توسط کاربر |
| تمرین | جلسات تمرینی، تمرینات انجام‌شده، ست‌ها و تکرارها | تا زمان حذف توسط کاربر |
| عادت‌ها | عادت‌های سفارشی کاربر و وضعیت روزانه | تا زمان حذف توسط کاربر |
| دستاوردها | نشان‌های کسب‌شده بر اساس داده‌های ورودی | تا زمان حذف توسط کاربر |
| تنظیمات | یادآوری‌ها، واحد نمایش، تم | تا زمان حذف توسط کاربر |

### ۱.۲ داده‌های دیتابیس غذایی (Pre-loaded)

اپلیکیشن شامل یک دیتابیس غذایی از پیش بارگذاری‌شده است که شامل حدود ۷,۵۷۵ ماده غذایی از منابع زیر است:

- **USDA FoodData Central** (United States Department of Agriculture)
  - Foundation Foods (نسخه 2026-04-30)
  - SR Legacy (نسخه 2018-04)
  - FNDDS / Survey (نسخه 2024-10-31)
- **منابع ایرانی مورد اعتماد** (مقادیر تغذیه‌ای دست‌ساز Bergamot)
- **۱۷ دستور پخت ایرانی** (محاسبه‌شده از مواد اولیه واقعی)

این دیتابیس به‌صورت یک فایل JSON درون خود اپلیکیشن بسته‌بندی شده و نیازی به اتصال اینترنت ندارد. منبع هر رکورد در فیلد `source` ذخیره شده است.

### ۱.۳ داده‌های فنی

اپلیکیشن ممکن است داده‌های فنی زیر را به‌صورت محلی ذخیره کند:

- نسخه اپلیکیشن و schema دیتابیس (برای migration)
- تاریخ آخرین باز شدن اپلیکیشن (برای نمایش "خوش آمدید")
- آخرین جستجوهای انجام‌شده (برای تاریخچه جستجو)
- وضعیت onboarding (آیا کاربر معرفی اولیه را دیده است؟)

این داده‌ها برای عملکرد اپلیکیشن ضروری هستند و هرگز به سرور ارسال نمی‌شوند.

---

## ۲. داده‌هایی که ارسال یا به اشتراک گذاشته نمی‌شوند

صراحتاً اعلام می‌شود که برگاموت **هیچ‌کدام** از موارد زیر را انجام نمی‌دهد:

- ❌ ارسال داده‌های سلامتی به سرور ما
- ❌ ارسال داده‌های سلامتی به اشخاص ثالث
- ❌ فروش داده‌های کاربر به تبلیغ‌کنندگان
- ❌ استفاده از Google Analytics یا Firebase Analytics
- ❌ استفاده از Crashlytics یا ابزارهای مشابه (مگر با رضایت صریح کاربر)
- ❌ استفاده از AI / LLM / ML برای تحلیل داده‌های کاربر
- ❌ تشخیص غذا از تصویر
- ❌ تخمین کالری از تصویر
- ❌ ردیابی مکان کاربر
- ❌ دسترسی به دوربین برای تحلیل (به‌جز Export PDF که در بخش ۴ توضیح داده شده)
- ❌ دسترسی به فایل‌های شخصی کاربر

---

## ۳. دسترسی‌های اپلیکیشن

برگاموت فقط دسترسی‌های ضروری زیر را درخواست می‌کند:

| دسترسی | دلیل استفاده | قابل غیرفعال‌سازی؟ |
|---|---|---|
| ذخیره‌سازی (Storage) | ذخیره دیتابیس محلی، Export PDF، Backup | خیر — برای عملکرد اصلی ضروری است |
| نوتیفیکیشن (Notifications) | یادآوری آب، خواب، تمرین، غذا | بله — از تنظیمات اپ |
| لرزش (Haptic) | بازخورد لمسی هنگام تکمیل اکشن‌ها | بله — از تنظیمات دستگاه |
| 시스템 Boot | راه‌اندازی مجدد نوتیفیکیشن‌ها پس از ری‌استارت | بله — از تنظیمات دستگاه |

**هیچ دسترسی به اینترنت برای عملکرد اصلی لازم نیست.** اپلیکیشن کاملاً آفلاین کار می‌کند.

---

## ۴. Export / Import داده‌ها

برگاموت به کاربر اجازه می‌دهد داده‌های خود را به دو روش Export کند:

### ۴.۱ Export JSON

تمام داده‌های کاربر در یک فایل JSON خروجی گرفته می‌شوند. این فایل شامل تمام جداول دیتابیس است (پروفایل، وزن، خواب، تغذیه، تمرین و ...).

**کاربرد:** Backup شخصی، انتقال به دستگاه دیگر، یا بازبینی دستی داده‌ها.

### ۴.۲ Export PDF Report

یک گزارش هفتگی یا ماهانه PDF شامل نمودارها و آمار خلاصه. این گزارش فقط شامل داده‌های جمع‌آوری‌شده (مجموع کالری، میانگین خواب و ...) است، نه داده‌های خام.

**کاربرد:** اشتراک‌گذاری با پزشک یا مربی شخصی (با رضایت کاربر).

### ۴.۳ Backup دیتابیس

یک کپی خام از فایل `bergamot.db` به‌عنوان backup. این فایل فقط برای بازیابی در همان دستگاه یا دستگاه جدید است.

**کاربرد:** بازیابی کامل داده‌ها پس از نصب مجدد.

### ۴.۴ اشتراک‌گذاری

وقتی کاربر از Export PDF یا Backup استفاده می‌کند، اپلیکیشن از `share_plus` (Flutter standard) استفاده می‌کند. این به کاربر اجازه می‌دهد فایل را با ایمیل، پیام‌رسان یا سرویس ذخیره‌سازی ابری به اشتراک بگذارد. **هیچ اشتراک‌گذاری خودکار انجام نمی‌شود** — کاربر باید به‌صورت دستی اقدام کند.

---

## ۵. امنیت داده‌ها

- دیتابیس محلی با SQLite ذخیره می‌شود و از طریق سیستم‌عامل دستگاه محافظت می‌شود.
- هیچ رمزنگاری اضافه در سطح اپلیکیشن انجام نمی‌شود، زیرا داده‌ها روی دستگاه خود کاربر هستند و کاربر می‌تواند با قفل صفحه‌نمایش دستگاه خود را محافظت کند.
- هیچ password یا credential شخص ثالث در اپلیکیشن ذخیره نمی‌شود.
- هیچ API key حساسی در کد اپلیکیشن embed نشده است.

---

## ۶. حذف داده‌ها

### ۶.۱ حذف یک بخش

کاربر می‌تواند هر بخش از داده‌های خود را به‌صورت جداگانه حذف کند:
- حذف یک وعده غذایی خاص
- حذف یک جلسه تمرین
- حذف یک عادت
- حذف یک رکورد وزن

### ۶.۲ حذف کامل (Reset)

از تنظیمات اپلیکیشن، کاربر می‌تواند:
- تمام داده‌های سلامتی را پاک کند (با تأیید دوگانه)
- دیتابیس را به حالت اولیه برگرداند
- اپلیکیشن را به حالت factory reset ببرد

### ۶.۳ حذف اپلیکیشن

اگر کاربر اپلیکیشن را uninstall کند، تمام داده‌های محلی به‌صورت خودکار توسط سیستم‌عامل حذف می‌شوند (مگر اینکه از "Auto Backup" در Android استفاده کند که در این صورت یک نسخه رمزنگاری‌شده از داده‌های اپلیکیشن در Google Drive کاربر ذخیره می‌شود — این قابلیت توسط سیستم‌عامل کنترل می‌شود نه توسط اپلیکیشن).

---

## ۷. کودکان

برگاموت برای استفاده عمومی بزرگسالان طراحی شده است. اپلیکیشن داده‌هایی را که به‌طور خاص از کودکان زیر ۱۳ سال جمع‌آوری کند، ندارد. اگر.parent به این نتیجه می‌رسد که فرزند زیر ۱۳ سال او داده‌هایی را بدون اجازه در اپلیکیشن ثبت کرده، می‌تواند با uninstall اپلیکیشن تمام داده‌ها را حذف کند.

---

## ۸. تغییرات این Privacy Policy

اگر این Privacy Policy تغییر کند، نسخه جدید همراه با هر آپدیت اپلیکیشن به کاربر نمایش داده می‌شود. نسخه فعلی همیشه درون خود اپلیکیشن (صفحه Privacy) قابل مشاهده است.

---

## ۹. تماس

برای سوالات درباره Privacy Policy، می‌توانید از طریق صفحه GitHub پروژه تماس بگیرید (آدرس در README.md).

---

## ۱۰. منابع و Attribution

داده‌های دیتابیس غذایی از منابع زیر گرفته شده‌اند:

- **USDA FoodData Central** — Public Domain (دولت فدرال آمریکا)  
  https://fdc.nal.usda.gov/
- **منابع ایرانی مورد اعتماد Bergamot** — دست‌ساز، نگهداری‌شده توسط پروژه  
  لیست کامل در `DATA_SOURCES.md`
- **دستور پخت‌های ایرانی** — دستورهای خانگی استاندارد، مقادیر تقریبی

---

## English Summary (for international users)

Bergamot is a privacy-first, offline-first health tracking app. It does NOT:

- Send any health data to any server
- Use AI, ML, or LLM
- Use Google Analytics, Firebase, or any third-party tracking
- Request internet permission for core functionality
- Track user location
- Sell user data to advertisers

All user data is stored locally on the device in a SQLite database. Users can export their data as JSON or PDF, and can delete all data at any time from the app settings.

The food database (≈7,575 foods) is bundled with the app as a JSON file and includes data from USDA FoodData Central (public domain) and curated Persian-language references. No internet access is needed to use the app.

---

## ⚠️ اعلام مهم

**این فایل یک پیش‌نویس است.** قبل از انتشار عمومی اپلیکیشن، این Privacy Policy باید توسط شما (مالک پروژه) یا یک مشاور حقوقی آشنا به قوانین حریم خصوصی ایران و کشورهای هدف بازبینی شود. نکات خاص که باید بررسی شوند:

1. **تطابق با قوانین ایران** — به‌ویژه قانون جرایم رایانه‌ای و قوانین حفاظت از داده‌های شخصی.
2. **تطابق با GDPR** اگر در اتحادیه اروپا توزیع می‌شود.
3. **تطابق با CCPA** اگر در کالیفرنیا توزیع می‌شود.
4. **تطابق با قوانین COPPA** برای بخش کودکان.
5. **مشخص کردن دقیق اینکه** آیا Crashlytics یا ابزارهای مشابه در نسخه production فعال می‌شوند یا خیر (در نسخه فعلی خاموش هستند).
6. **آدرس دقیق تماس** (نه فقط GitHub) برای کاربرانی که سوال privacy دارند.
7. **سیاست نگهداری داده** — مدت زمان نگهداری داده‌های کاربر اگر کاربر اپلیکیشن را پاک نکند.
8. **سیاست رضایت (Consent)** — آیا رضایت کاربر در زمان onboarding ثبت می‌شود یا صرفاً با استفاده از اپلیکیشن به‌عنوان رضایت در نظر گرفته می‌شود.

این موارد توسط توسعه‌دهنده/مالک پروژه باید تکمیل و به‌صورت حقوقی تأیید شوند.
